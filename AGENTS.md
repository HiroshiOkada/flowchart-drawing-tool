# 作業ルール

開始時は作業ツリーとブランチを確認する。未コミット変更は提示し、許可後にコミット（拒否なら中断）。`main` 以外なら `main` に切り替え、現ブランチをマージする。

軽微な修正（ドキュメント・設定など）はタスク文書も作業ブランチも作らず、現ブランチへ直接コミットする。

機能開発は先に `.tasks/NNN-nnn_内容.md` を置く。`NNN` はフェーズ、`nnn` は文書番号。`000-000` がゴール、`000-001` 以降がチェックリスト付き計画。次フェーズは `001-000` から。作業ブランチを切り、タスク文書を最初のコミットにする。以降はチェックリスト1項目ごとに実装し、成果物と `[x]` を同じコミットに残す。テストの作成・実行は独立項目とし、複数の実装の後にまとめてよい。対象はコアロジック（計算・入出力・不正入力）の代表例に限り、UI網羅や過剰なケースは書かない。重要な段階では統合テストを行い、Webアプリの場合できるだけ実ブラウザ又はそれに近い環境でテストする。この場合 Agent の能力を使ってUIをチェックし、この統合テストは `main` にマージ前に通す。手戻りは計画を更新してコミット。方針変更なら更新をコミットして `main` へマージする。

秘密値は `.env` から読み、リポジトリに入れない。

Python は uv、Node.js は npm。文書・会話・コード内説明は日本語。コミットメッセージは英語。
